// V1: pas de table learning_path_reviews exposée — section masquée par défaut.
// TODO: brancher sur learning_path_reviews quand la table sera publique.

import { LearningPath } from "@/lib/supabase/types"

interface ParcoursTestimonialsProps {
  learningPath: LearningPath
}

export function ParcoursTestimonials(_props: ParcoursTestimonialsProps) {
  return null
}
